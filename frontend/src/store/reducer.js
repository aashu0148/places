const initialState = {
  id: "uid_2",
  name: "sonu",
  places: ["pid_1"],
  fav: ["pid_2"],
  userPhoto: "",
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
